{/* <Card>
            <CardHeader>
              <CardTitle className="flex items-center justify-between">
                추가옵션
                <Button type="button" size="sm" onClick={() => handleAddOption('additionalOptions')}>
                  <Plus className="h-4 w-4 mr-2" />
                  추가옵션 추가
                </Button>
              </CardTitle>
              <CardDescription>상품의 추가 옵션을 설정하세요.</CardDescription>
            </CardHeader>
            <CardContent className="space-y-4">
              {additionalOptions.map((option) => (
                <div key={option.id} className="p-4 border rounded-lg space-y-3">
                  <div className="flex items-center justify-between">
                    <Input
                      placeholder="추가옵션명 (예: 보증기간, 설치서비스)"
                      value={option.name}
                      onChange={(e) => handleOptionNameChange(option.id, e.target.value, 'additionalOptions')}
                    />
                    <Button
                      type="button"
                      variant="ghost"
                      size="sm"
                      onClick={() => handleRemoveOption(option.id, 'additionalOptions')}
                    >
                      <X className="h-4 w-4" />
                    </Button>
                  </div>
                  <div className="space-y-2">
                    {option.values.map((value, index) => (
                      <div key={index} className="flex gap-2">
                        <Input
                          placeholder="추가옵션값"
                          value={value}
                          onChange={(e) =>
                            handleOptionValueChange(option.id, index, e.target.value, 'additionalOptions')
                          }
                        />
                        <Button
                          type="button"
                          variant="outline"
                          size="sm"
                          onClick={() => handleAddOptionValue(option.id, 'additionalOptions')}
                        >
                          <Plus className="h-4 w-4" />
                        </Button>
                        {option.values.length > 1 && (
                          <Button
                            type="button"
                            variant="ghost"
                            size="sm"
                            onClick={() => handleRemoveOptionValue(option.id, index, 'additionalOptions')}
                          >
                            <X className="h-4 w-4" />
                          </Button>
                        )}
                      </div>
                    ))}
                  </div>
                </div>
              ))}
              {additionalOptions.length === 0 && (
                <p className="text-sm text-muted-foreground text-center py-4">
                  추가옵션을 설정하여 고객에게 더 많은 선택권을 제공하세요.
                </p>
              )}
            </CardContent>
          </Card> */}